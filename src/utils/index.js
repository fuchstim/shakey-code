const cli = require('./_cli');
const log = require('./_log');
const image = require('./_image');
const ColourEncoder = require('./_colour-encoder');

module.exports = {
  cli,
  log,
  image,
  ColourEncoder,
};
